</Accordion>

<Box
  sx={{
    display: "flex",
    justifyContent: "flex-end",
    mt: 2,
    mb: 2,
  }}
>
  <Button
    variant="contained"
    color="success"
    onClick={handleExport}
    sx={{ textTransform: "none" }}
  >
    Export
  </Button>
</Box>

<Box sx={{ height: 550 }}>
  <DataGrid
    rows={rows}
    columns={columns}
    getRowId={(row) => row.id}
    loading={loading}
  />
</Box>