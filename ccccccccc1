<FormHelperText>
  {errors.categoryId
    ? errors.categoryId
    : "Select the voucher category to create the voucher request."}
</FormHelperText>


helperText={
  errors.issues
    ? errors.issues
    : "Select one or more issues. If not available, choose Add New Category."
}

<FormHelperText>
  {errors.roleId
    ? errors.roleId
    : "Select the role that will be allowed to process this request."}
</FormHelperText>

helperText={
  errors.otherCategory
    ? errors.otherCategory
    : `Enter a new voucher category. Only letters, numbers, hyphen and single space allowed. (${formData.otherCategory.length}/20)`
}


helperText={
  errors.sdRequestNumber
    ? errors.sdRequestNumber
    : `Enter one or more SD request numbers separated by commas. (${formData.sdRequestNumber.length}/20)`
}


helperText={
  errors.description
    ? errors.description
    : `Describe the voucher request. All characters are allowed. (${formData.description.length}/500)`
}


helperText={
  errors.customIssue
    ? errors.customIssue
    : `Enter a new issue if it is not available in the list. (${customIssueInput.length}/100)`
}



const sortedCategories = [...categories].sort((a, b) => {
  if (a.categoryName === "Other") return 1;
  if (b.categoryName === "Other") return -1;
  return 0;
});

setVoucherCategories(sortedCategories);


<MenuItem
  key={item.id}
  value={item.id}
>
  {item.categoryName === "Other"
    ? "Add New Category"
    : item.categoryName}
</MenuItem>

selectedCategory?.categoryName === "Other"




slotProps={{
  textField: {
    size: "small",
    fullWidth: true,
    helperText: "Select a date for search",
    onKeyDown: (e) => e.preventDefault(),
    inputProps: {
      readOnly: true,
    },
  },
}}