import { useNavigate } from "react-router-dom";
import { useSelector } from "react-redux";
import { findMenuById } from "../.<Button
  variant="contained"
  onClick={() => {
    setOpenDialog(false);

    const viewMenu = findMenuById(menuItems, <VIEW_REQUEST_MENU_ID>);

    navigate(viewMenu?.route || selectedMenuItem?.route);
  }}
>
  View Requests
</Button>./Utility/CommonUtilities";

const navigate = useNavigate();

const menuItems = useSelector((state) => state.menu.menuList);
const selectedMenuItem = useSelector(
  (state) => state.menu.selectedMenuItem
);

